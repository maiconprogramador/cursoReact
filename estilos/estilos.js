import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
        backgroundColor: '#ddd'
    },
    textoCursos: {
        color: '#00f',
        fontSize: 15
    },
    textoTitulo: {
        color: '#000',
        fontSize: 23
    }
});